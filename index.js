const inquirer = require('inquire');
const fs = require('fs');
const generateLogo = require('./lib/generateLogo')
inquirer
    .prompt(questions)
    .then((answers) => {
        {}
        const logo = 
    })